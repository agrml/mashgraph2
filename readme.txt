ФИО: Аграновский Михаил Леонидович
Группа: 321

Выполнены все 3 части работы (HOG, LBP, COLOR).

note: в каркасе auto_ptr заменены на unique_ptr. C++11!!!

Во всех частях изображение делится на 64 (8*8) клетки. Отрезок значений угла в HOG разбивается на 8 подотрезков.
note: При желании, Вы можете изменить эти значения. Они хранятся в константах:
    constexpr uint8_t N_SQUARES_PER_LINE = 8;
    constexpr uint8_t HIST_SZ = 8;
Перед разбиением на клетки изображение расширяется так, чтобы поделиться нацело. Ввыброно дополнение константой (конкретно, нулем) -- чтобы вклад краев не превосходил вклад других клеток.

Note: В локальных биномиальных признаках соседи пиксля перебираются не по часовой стрелке, а по порядку (проход по матрице сверху-вниз слева-направо).

Если Вам нужно проверить работу не всех частей вместе, закомментируйте в теле функции `ExtractFeatures` строки 268, 269 или 270.
Например, для отключения LBP:
    //desc.insert(desc.end(), lbpDesc.begin(), lbpDesc.end());

Оценка точности работы предсказаний на предложенном тесте:
HOG + LBP + COLOR:  0.969697
HOG:                0.888889
LBP:                0.939394
COLOR:              0.666667
Время работы (HOG + LBP + COLOR: построение модели + предсказание): ~4 секунды (i5 6300HQ, 8GB RAM). Дамаю, Ваш Dell справится.
